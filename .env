# 编译时候sqlx会宏展开静态检查数据库中对应表和字段是否正确，如果连接不上数据库，编译会失败。

# 注意：这个文件在编译时候很重要，因为sqlx会静态检查数据库的表结构是否一致，所以这里的设置要能够连接上正确的数据库
# DATABASE_URL=mysql://用户名:密码@ip地址:端口/weihuda
DATABASE_URL=mysql://weihuda:weihuda_testing_only@171.80.1.44:3306/weihuda
# Load the environment variables from .env file
# dotenv().ok();
# Get the config from the environment variables
# let database_url = std::env::var("DATABASE_URL").expect("DATABASE_URL must be set");
